# Gemini CLI 규칙

## 코드베이스 컨텍스트
- `ARCHITECTURE.md`에 정의된 도메인 주도 아키텍처를 따릅니다.
- 모든 새로운 기능은 각각의 도메인 패키지 내에 추가되어야 합니다.
- 새로운 도메인이 도입될 경우, 표준 도메인 하위 패키지 구조를 따라야 합니다.

## 운영 명령 (Mandates)
- **임포트(Import) 항상 업데이트**: 클래스를 이동하거나 추가할 때, 프로젝트 전반의 모든 임포트가 올바르게 업데이트되었는지 확인합니다.
- **문서화 유지**: 도메인의 서비스 정책이나 구조가 변경되면 해당 도메인의 `README.md`를 업데이트합니다.
- **일관성**: `ARCHITECTURE.md`에 지정된 Kotlin 스타일 및 명명 규칙을 준수합니다.
- **검증**: 구조적 변경 후에는 `find`를 사용하여 패키지 구조를 확인하고 프로젝트가 정상적으로 빌드되는지 확인합니다.
- **보안 및 빌드 안정성**: 
    - `application.properties`의 환경 변수(예: `${VAR}`)는 반드시 기본값(예: `${VAR:default}`)을 제공하여 로컬 개발 및 빌드 환경에서 컨텍스트 로딩 실패를 방지합니다.
    - 비밀번호나 환경 변수 정보를 로그나 문서에 노출하지 않습니다.

## 데이터 처리 및 스토리지 패턴
- **Base64 정책**: 테스트케이스(Input/Output) 및 제출된 소스코드는 공백과 줄바꿈 보존을 위해 Base64 인코딩 상태로 Object Storage에 저장합니다.
- **디코딩 책임**: 서버는 Storage에서 읽어온 Base64 데이터를 그대로(또는 String으로 읽어서) 반환하며, 실제 렌더링을 위한 **디코딩은 클라이언트가 담당**합니다.
- **스토리지 URL 처리**: `OracleStorageUtil`을 사용하여 URL에서 `objectName`을 추출할 때, 중첩 인코딩(Double Encoding) 방지를 위해 반드시 `java.net.URLDecoder.decode()`를 호출한 후 SDK에 전달해야 합니다.
- **의존성 관리**: Spring Cloud 관련 의존성(Feign 등)은 버전 충돌 방지를 위해 `build.gradle.kts`에서 Spring Cloud BOM을 통해 관리합니다.

## 코드 생성 지침
- Gemini는 코드를 생성하거나 수정할 때 반드시 프로젝트 루트의 `ARCHITECTURE.md`, `GEMINI.md` 및 해당 도메인의 `README.md`를 참고해야 합니다.
- 기존의 설계 원칙과 명명 규칙을 엄격히 준수하여 일관성 있는 코드를 작성합니다.

## AI-DLC 개발 프로세스 (기능 개발/수정 프로세스)
앞으로의 모든 기능 구현, 개발, 버그 수정 등의 작업 요청 시 아래의 **AI-DLC (AI Software Development Life Cycle)** 프로세스를 엄격히 준수하여 작업을 진행합니다.

1. **분석 및 구현 계획 작성 (Research & Plan)**
   - 요구사항을 분석하고 관련 코드베이스 및 영향 범위를 파악합니다.
   - 실제 소스코드 수정을 진행하기 전에, 구현 계획 초안을 작성하여 **`implementation_plan.md`** 아티팩트 파일로 생성 또는 업데이트합니다.
   - 계획서에는 다음 항목이 필수로 포함되어야 합니다:
     - **목표 및 상세 설계**: 변경 목적 및 세부 설계 방향.
     - **변경 대상 파일 및 컴포넌트**: 신규 추가(`[NEW]`), 수정(`[MODIFY]`), 삭제(`[DELETE]`) 대상.
     - **검증 계획**: 테스트 코드 빌드 및 수동 검증 방법.
     - **사용자 확인 필요 사항 및 질문(Open Questions)**: 설계 의도나 대안 등 조율할 부분.

2. **사용자 피드백 및 최종 승인 (Feedback & Approval)**
   - `implementation_plan.md` 초안이 완성되면 사용자에게 이를 제안하고 피드백을 요청합니다.
   - **사용자의 명시적인 승인(Approve)을 받기 전까지는 절대 본 소스코드를 변경하지 않습니다.**
   - 사용자의 수정 의견이 있을 시 계획서를 갱신하여 재승인을 받습니다.

3. **구현 및 작업 관리 (Execute & Track)**
   - 승인 완료 후 구현 단계로 진입하며, 작업을 체계적으로 관리하기 위해 **`task.md`** 아티팩트 파일을 생성합니다.
   - 작업 내용을 태스크 단위로 세분화하여 진행 상태(미진행 `[ ]`, 진행 중 `[/]`, 완료 `[x]`)를 지속해서 반영합니다.

4. **검증 및 최종 결과 공유 (Verify & Walkthrough)**
   - 작업 완료 후 계획된 검증 방식에 따라 정상 동작 및 빌드 안정성을 확인합니다.
   - 구현이 완료되면 변경점과 테스트 검증 결과를 정리한 **`walkthrough.md`** 아티팩트 파일을 작성하여 사용자에게 보고합니다.
